
<ul class='nav'>
    <li>
        <div class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" href="#">Erstellen</a>
            <ul class="dropdown-menu" role="menu" aria-labelledby="dLabel">
                <li><a href="#">AML Generieren</a></li>
                <li class="divider"></li>
                <li>Download</li>
                <li>Upload</li>
            </ul>
        </div>
    </li>
</ul>

<ul class='nav'>
    <li>
        <div class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" href="#">Weiter testen</a>
            <ul class="dropdown-menu" role="menu" aria-labelledby="dLabel">
                <li><a href="#">AML Generieren</a></li>
                <li class="divider"></li>
                <li>Download</li>
                <li>Upload</li>
            </ul>
        </div>
    </li>
</ul>

<hr />

<form asp-action="CreateEdit">
    <button type="submit" class="btn btn-primary">+ Create Project</button>
</form>


<hr />


<table class="table table-dark table-striped table-hover">
    <thead>
        <tr>
            <th>ID</th>
            <th>Projekt Nummer</th>
            <th>Name</th>
            <th>Kunde</th>
            <th>Bearbeiten</th>
            <th>Löschen</th>
        </tr>
    </thead>

    <tbody>
        @{ foreach (Projekt projekt in ViewBag.Projekte)
            {
                <tr>
                    <td>
                        @projekt.Id
                                           
                    </td>
                    <td>
                        @projekt.ProjektNummer
                    </td>
                    <td>
                        @projekt.Name
                    </td>
                    <td>
                        @projekt.Kunde
                    </td>
                    <td>
                        <form asp-action="CreateEdit" asp-route-id="@projekt.Id">
                            <button type="submit" class="btn btn-warning">Edit</button>
                        </form>

                    </td>
                    <td>
                        @if (User.IsInRole("Admin"))
                        {
                            <form asp-action="DeleteProjekt" asp-route-id="@projekt.Id">
                                <button type="submit" class="btn btn-danger">Delete</button>
                            </form>
                        }
                    </td>

                </tr>
            }
        }
    </tbody>
</table>
